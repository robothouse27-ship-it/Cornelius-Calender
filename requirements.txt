flask>=3.0
requests>=2.31
icalendar>=5.0
recurring-ical-events>=2.2
tzdata>=2024.1
qrcode[pil]>=7.4

# Photo capture → list reading (Stage B). Pillow ships with qrcode[pil] above;
# anthropic powers the cloud vision read, pytesseract is the free on-box fallback.
anthropic>=0.40
Pillow>=10
pytesseract>=0.3

# Local voice control (Stage C) lives in requirements-voice.txt — it pulls
# heavy, CPU-architecture-specific wheels (tflite-runtime via openwakeword)
# that don't install on every box, so it's opt-in and kept out of the core set.
