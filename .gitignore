__pycache__/
*.pyc
.venv/
venv/
data/events.json
data/export.json
data/*.tmp
data/server.log
.DS_Store

# family photos are personal — keep them out of git, but keep the folder
photos/*
!photos/README.txt

# stray images dropped in the project root (keep them local, out of git)
/*.png
data/feeds.local.json

# raw spec drop (zip + unpacked dupes); the canonical copies live as docs-*.md
/files/
